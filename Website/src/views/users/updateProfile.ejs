<!DOCTYPE html>
<html lang="es">
  <%- include('../partials/head') %>
  <body>
    <%- include('../partials/header') %>
    <main>
      <h2>
        Hola, soy <%= user.email %> <% if (user.isAdmin === true) { %> y soy
        admin <i class="fa-solid fa-face-sunglasses"></i> <% } %>
      </h2>
      <% if (user.image_id != null) { %>
        <picture>
          <img src="/img/Usuarios/<%=user.images.url%> " alt="Avatar <%= user.email %> " />
        </picture>
      <% } else {%>
        <picture>
          <img src="/img/Usuarios/genericUser.jpeg" alt="Imagen Genérica" />
        </picture>
        <% } %> 
        <p>
          Actualiza tus datos !
        </p>
        <form action="/updateProfile>?_method=PUT" method="POST" class ="form-box-user edit" enctype="multipart/form-data">
           
                <div class="errors" style="color: red;">
                    <ul></ul>
                </div>
            <br>    
                <input type="text" name="firstName" placeholder="Nombre"> <!-- agregar value para capturar los datos de la base en el input -->
                <% if (locals.errors && errors.name) { %>
                    <div class="text-danger">
                       <%= errors.name.msg %>
                  </div>
                    <% } %>

            <input name="lastName" placeholder="Apellido" ></input>
            <% if (locals.errors && errors.description) { %>
                <div class="text-danger">
                   <%= errors.description.msg %>
              </div>
                <% } %>

            <input type="text" name="email" placeholder="e-mail">
            <% if (locals.errors && errors.category) { %>
                <div class="text-danger">
                   <%= errors.category.msg %>
              </div>
                <% } %>

                <label>Elija una o más imágenes</label>
            
            <input type="file" name="image_id" placeholder="Imagen">
            <% if (locals.errors && errors.productimage) { %>
                <div class="text-danger">
                   <%= errors.productimage.msg %>
              </div>
                <% } %>
            </form>
            <input type="submit" value="Enviar">

            

          <!--   <form action="/admin/borrar/" method="POST" class ="form-box-admin">
                <input type="submit" value="ELIMINAR PRODUCTO">
            </form> -->
                
    </main>
        
    </main>
    <%- include('../partials/footer') %>
  </body>
</html>