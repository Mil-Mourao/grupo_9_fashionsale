<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %>

<body>
    <%- include('../partials/header') %>
    <main>
       
        <% if (user.image_id != null) { %>
          <!-- <div class="profileImage"></div>
        <picture>
            <img src="/img/Usuarios/<%=user.images.url%> " alt="Avatar <%= user.email %> " />
        </picture> -->
        <% } else {%>
          
      

        
      
        <% } %>
        <main>
            <section>
                <article id="update-form-complete">
                    <picture class="logo-form">
                        <img src="/img/logo/logoSimple.svg" alt="" id="update-form">
                    </picture>
                </article>

                <form action="/updateProfile>?_method=PUT" method="POST" id="form" enctype="multipart/form-data">
                    <fieldset>
                        <h3 id="form-title">¡Modifica tus datos!</h3>
                        <div class="errors" style="color: red;">
                            <ul></ul>
                        </div>
                        <br>
                    </fieldset>
                    <fieldset>
                        <input type="text" firstName="name" placeholder="Nombre" id="name"> <!-- agregar value para capturar los datos de la base en el input -->
                        <% if (locals.errors && errors.name) { %>
                        <div class="text-danger">
                            <%= errors.name.msg %>
                        </div>
                        <% } %>
                    </fieldset>

                    <fieldset>

                        <input name="lastName" placeholder="Apellido" id="lastName"></input>
                        <% if (locals.errors && errors.description) { %>
                        <div class="text-danger">
                            <%= errors.description.msg %>
                        </div>
                        <% } %>
                    </fieldset>
                    <fieldset>

                        <input type="text" name="email" id="email" placeholder="e-mail">
                        <% if (locals.errors && errors.category) { %>
                        <div class="text-danger">
                            <%= errors.category.msg %>
                        </div>
                        <% } %>
                    </fieldset>
                    <input type="submit" value="Enviar" id="button-enviar">
        </main>
        </section>
        </form>
        



        <!--   <form action="/admin/borrar/" method="POST" class ="form-box-admin">
                <input type="submit" value="ELIMINAR PRODUCTO">
            </form> -->

    </main>

    </main>
    </main>
    <%- include('../partials/footer') %>
</body>

</html>