<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head') %>

  <body>
    <%- include('./partials/header') %>
      <main>
        <section id="banner">
          <picture>
            <img src="/img/Splash/Splash-1.jpg" alt="banner">
          </picture>
        </section>
        <section class="main_section">
          <% if (locals.products && locals.products.length> 0) { %>
            <h2 class="title-oferta">Resultados en base a tu búsqueda: <%=search%> </h2>
            <% products.forEach((element)=> { %>
              <article class="card"> 
                <picture>
                  <a href="/products/<%= element.id %> ">
                    <% if (element.images !=null && element.images.length> 0 )
                      { %> <% for( let i=0; i < element.images.length; i++ ) { %>
                        <% if (i<1) { %>
                          <img src="/img/Productos/<%= element.images[0]['url']%>" alt="<%= element.name %> " />
                          <% } %>
                            <% } %>
                              <% } else {%>
                                <img src="/img/Productos/default.jpg" alt="<%= element.name %>" />
                                <% } %>
                  </a>
                </picture>
                <ul>
                  <li>
                    <%= element.name %>
                  </li>
                  <li>
                    $ <% if (element.ofert) { %>
                      <%= parseInt(element.price * ((100 - element.discount) / 100)) %>
                        <% } else {%>
                          <%= element.price %>
                            <% } %>
                  </li>            
                </ul>
              </article>
              <% }) %>
                <% } else {%>
                  <h2 class="title-oferta">Atención!!</h2>
                  <p>No se encuentran productos en base a tu búsqueda.</p>                
                  <p>Prueba con otro artículo por ejemplo "campera".</p>                 
                <% } %>
        </section>
      </main>
      <%- include('./partials/footer') %>
  </body>

</html>